# IA Premium (Claude)
ANTHROPIC_API_KEY=sk-ant-xxxxx
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# IA Provider Selection
# Options: "claude" (défaut) | "local" (Ollama/DeepSeek)
AI_PROVIDER=claude

# IA locale (Ollama / DeepSeek)
# Décommentez ces lignes si vous utilisez AI_PROVIDER=local
# OLLAMA_URL=http://localhost:11434
# LOCAL_AI_MODEL=deepseek-coder-v2

# Mode sans IA (pour tests et développement)
# Si défini à "true", aucun appel IA ne sera effectué (fallback interne)
NO_AI=false

# Vercel Configuration (optionnel)
VERCEL_TOKEN=your_vercel_token_here

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Génération
MAX_SITES_PER_DAY=100
ZIP_STORAGE_PATH=/tmp/generated-sites

# Debug
NODE_ENV=development

# Authentication
# Session secret for signing cookies (change this in production!)
SESSION_SECRET=change-this-to-a-random-secret-in-production-min-32-chars
